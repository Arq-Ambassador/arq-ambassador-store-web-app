apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: store-web-app-store
  namespace: ambassador-test
spec:
  virtualhost:
    fqdn: store.ambassadors.ink
  routes:
    - conditions:
      - prefix: /
      requestRedirectPolicy:
        hostname: www.ambassadors.ink